<ul class="nav nav-tabs">
    {{#each collections.pages }}
        <li class="nav-item">
            <a href="{{ this.path }}" data-toggle="tab"
                class="nav-link{{#equals ../title title}} active{{/equals}}" >
                {{ this.short_title }}
            </a>
        </li>
    {{/each}}
</ul>